package scraper;

public enum CaseOfMatching {
	sameNameOneMatch, differentNameOneMatch, 
	sameNameSeveralMatch, differentNameSeveralMatch,
	differentNameNoMatch, unknowCase, perfectMatch,
	noSubmitOneMatch, noSubmitSeveralMatch,noSubmitNoMatch;
}
